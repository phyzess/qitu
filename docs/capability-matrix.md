# Capability Matrix

Status: draft  
Date: 2026-07-10

Legend:

| Status           | Meaning                                                       |
| ---------------- | ------------------------------------------------------------- |
| Designed         | Architecture and decisions are documented.                    |
| Scaffolded       | Package or app shell exists.                                  |
| Runnable         | Local command path exists.                                    |
| Verified         | Covered by smoke, type, unit, integration, or runtime checks. |
| Production-ready | Ready for real users with operational runbooks.               |

## Current Matrix

| Capability            | Designed | Scaffolded | Runnable | Verified | Production-ready | Notes                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --------------------- | -------- | ---------- | -------- | -------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| React app shell       | Yes      | Yes        | Yes      | Partial  | No               | API-backed auth and protected Workspace/Settings routes, persisted English/Simplified Chinese selection, role-aware controls, source/import/review/audit surfaces, and frontend-only demo mode are wired. `AppShell` adds skip navigation, document titles, route-change focus, native modified-link behavior, and optional non-duplicating route headings; shared workbench layouts and browser smoke cover the responsive shell. |
| Worker API shell      | Yes      | Yes        | Yes      | Partial  | No               | `apps/worker` exposes health/runtime primitives, HTTP and inbound-email source intake, Queue-backed processing, a post-response fast path with the durable Queue as fallback, explicit queued-job redispatch, processing/mutation leases, isolated Worker runtime configuration, and integration/runtime checks.                                                                                                                   |
| Auth                  | Yes      | Yes        | Partial  | Partial  | No               | Invite links, accept page, login, protected routing, account/logout, admin user/invitation management with resend/revoke/delete-revoked, member hard delete guards, reset page, session revocation, security events, and audit paths are integrated.                                                                                                                                                                               |
| RBAC                  | Yes      | Partial    | Partial  | Partial  | No               | `@qitu/rbac` defines reusable permission helpers and a starter role policy; deployable apps own their canonical role policy; Worker write routes enforce permissions and audit/security-event `rbac.denied`; the React shell projects viewer writes as read-only controls. Production multi-tenant scopes remain outside the default baseline; an isolated optional organization example is available.                             |
| Files                 | Yes      | Yes        | Yes      | Partial  | No               | Authenticated HTTP/email intake writes R2, D1, and audit evidence. Raw download/preview, reparse, source deletion, UTF-8-safe filenames, soft tombstones, compare-and-swap deletion claims, adapter-owned cleanup hooks, structured retry stages, and report-only retention are implemented and integration-tested.                                                                                                                |
| Jobs                  | Yes      | Partial    | Yes      | Partial  | No               | Upload creates a durable queued job and schedules a best-effort fast path. Ownership leases prevent duplicate processors; Queue remains the fallback, queued jobs can be redispatched, and failures expose class/reason plus recovery evidence through `import_job_events`. Review mutations use a separate lease and the explicit `committing` state to exclude concurrent writes.                                                |
| Import pipeline       | Yes      | Partial    | Yes      | Partial  | No               | App-owned text/JSON adapters use parse/stage/validate/`commitApproved`. Open record errors block approval and commit unless a single-record action explicitly accepts the observed errors. Adapters default to manual review and may opt deterministic clean imports into `commitPolicy: "auto_when_clean"`; auto commit reuses the guarded human-review persistence path and a system actor.                                      |
| Human review          | Yes      | Partial    | Yes      | Partial  | No               | Review, decision, confirmation, commit, retry, redispatch, void, and staged-record adjustment routes are wired. Compare-and-swap mutation claims exclude concurrent decisions/commits; adjustments retain before/after hashes, explicit error overrides are audited, and integration/browser checks cover approval, rejection, recovery, and commit paths.                                                                         |
| Audit/security/alerts | Yes      | Partial    | Partial  | Partial  | No               | Sensitive paths write audit, including invitation lifecycle events; login attempts, security events, import job events, alert events, list APIs, server-backed audit filters, and web timeline/detail views exist for runtime visibility.                                                                                                                                                                                          |
| Email                 | Yes      | Partial    | Partial  | Partial  | No               | Invitation/reset templates, `store` and `send` delivery modes, `email_messages` delivery ledger, visible failed invite delivery with resend, Cloudflare `send_email`, React token landing pages, and business-neutral inbound email handling are wired.                                                                                                                                                                            |
| AI advisory           | Yes      | Partial    | Partial  | Partial  | No               | Local deterministic import-review advisory artifacts, list/generate/confirm/dismiss routes, web panel, audit events, and browser smoke coverage for generate/confirm are wired.                                                                                                                                                                                                                                                    |
| Charts                | Yes      | Yes        | Yes      | Partial  | No               | `@qitu/charts` exports responsive, token-driven visx primitives with loading/empty/error/partial states, package-owned styles, pointer/focus tooltips, interactive legends, keyboard time-series inspection, live announcements, and reduced-motion behavior. Unit and browser checks cover the fragile interaction paths.                                                                                                         |
| Design system         | Yes      | Yes        | Yes      | Partial  | No               | Business-neutral tokens, accessible shell, `WorkbenchPage`/`WorkbenchGrid`/`ContextPanel`, surfaces, metrics, data states, timeline, calendar/date-field localization, and review compositions are implemented. Context layouts collapse below 1180px and shared motion honors reduced-motion preferences.                                                                                                                         |
| Internationalization  | Yes      | Yes        | Yes      | Partial  | No               | `@qitu/i18n` provides reusable locale primitives, plural/relative-time helpers, and locale negotiation; `apps/web`, Worker auth routes, and email templates own their dictionaries and selection UX.                                                                                                                                                                                                                               |
| Feature templates     | Yes      | Yes        | Yes      | Partial  | No               | `templates/app` has a verified copy manifest and adoption commands; `templates/feature` is a typechecked copyable adapter with migration, fixture, web-surface, registry replacement slots, and an optional versioned-derived-artifact recipe. Business formulas and artifact tables remain feature-owned.                                                                                                                         |
| Organization access   | Yes      | Yes        | Yes      | Partial  | No               | `examples/organization-access` is an isolated, executable opt-in example for organizations, memberships, entitlements, time-boxed read-only support access, and exact resource grants. Disabled organizations and unknown actions fail closed. It does not change the starter's single-organization default and is not a production tenant system.                                                                                 |
| Derived artifacts     | Yes      | Yes        | N/A      | Partial  | No               | The optional guide and feature recipe define calculation/source-data version checks, deterministic invalidation, rebuild triggers, and golden-fixture obligations. Qitu intentionally provides no generic metrics engine or core derived-artifact table.                                                                                                                                                                           |
| Agent docs            | Yes      | Yes        | N/A      | Partial  | No               | Codex, Claude Code, and Pi entrypoints exist.                                                                                                                                                                                                                                                                                                                                                                                      |
| Cloudflare deployment | Yes      | Partial    | Partial  | Partial  | No               | Cloudflare Pages static demo commands exist for frontend-only review; local, preview, and production binding stubs exist; dry-run preflight checks public origins, Email Sending config, D1 placeholders, static assets, and queue DLQ; DLQ remediation, email deliverability, and deployment runbooks are wired, but no real preview/production account is provisioned.                                                           |

## Verification Targets

Near-term verification should be added in this order:

1. Static smoke checks for template invariants.
2. Worker handler integration with local D1/R2/Queue fakes.
3. Package type checks with stable TypeScript 7.0.2.
4. Worker runtime tests covering Cloudflare Email Service, Queues, D1, and R2 binding behavior beyond the current minimal runtime smoke.
5. Broader adapter edge-case tests only when new real adapters add parser-specific risk.

## Production Readiness Gate

A capability becomes production-ready only when these are true:

1. It has tests that exercise success and failure paths.
2. It writes audit events for sensitive actions.
3. It has rate limits or abuse controls where relevant.
4. It has an operational recovery path.
5. It does not depend on AI output without human confirmation.
6. It has documented Cloudflare bindings and secrets.
